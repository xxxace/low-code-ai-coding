# 表单开发最佳实践

> 基于项目代码分析总结
> 分析时间：2026-03-24

---

## 一、目录结构标准

```
views/
└── inquiryOrder/              # 业务模块名
    ├── components/            # 子组件
    │   ├── InquiryOrderTable.vue
    │   └── PackageJobTable.vue
    ├── dialogs/               # 弹窗组件
    │   ├── customerDialog.vue
    │   └── factoryDialog.vue
    ├── form/                  # 表单主体
    │   ├── index.vue          # 表单组件
    │   ├── types.ts           # 类型定义
    │   ├── validationRules.ts # 验证规则
    │   └── relations/
    │       └── form.ts        # 表单关系配置
    ├── list.vue               # 列表页
    ├── router/
    │   └── index.js           # 路由配置
    └── utils.ts               # 工具函数
```

---

## 二、表单开发模板

### 2.1 表单组件模板

```vue
<template>
  <StdFormWrapper :toolbar="showToolbar">
    <div class="!min-w-[700px] relative">
      <!-- 字段组1 -->
      <div class="whitespace-nowrap mt-1">
        <FieldItem :label="t('lbPLNNO', '编号')" :width="56">
          <el-input class="!w-[120px]" v-model="form.PLNNO" readonly/>
        </FieldItem>
        
        <FieldItem :label="t('lbCLNT', '客户')" :width="56" required>
          <OptionInput
            v-model="form.CLNT"
            v-model:labelValue="form.V_CLNTNAME"
            :dialog-compoent="CustomerDialog"
            @change="handleCLNTChange"
          />
        </FieldItem>
      </div>
      
      <!-- 更多字段... -->
    </div>
  </StdFormWrapper>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import dayjs from 'dayjs'
import StdFormWrapper from '@/components/StdForm/index.vue'
import OptionInput from '@/components/OptionInput.vue'
import { useStdForm } from '@/components/StdForm/composeble/useStdForm'
import { useRefManager } from '@/hooks/useRefManager'
import { useSetupStdFormMeta } from '@/hooks/useSetupStdFormMeta'
import { useNsI18n } from '@/hooks/useNsI18n'
import { formRelation } from './relations/form'
import { formRules } from './validationRules'
import type { InquiryOrderVO } from './types'
import CustomerDialog from '../dialogs/customerDialog.vue'

// ==================== 配置 ====================
defineOptions({
  objectName: 'VUE_INQUIRY'
})

// ==================== Hooks ====================
const stdForm = useStdForm()
stdForm.toolbar.print = false

useSetupStdFormMeta(stdForm)
const { t, c } = useNsI18n('VUE_INQUIRY')

// ==================== 数据管理 ====================
const [form, formManager] = useRefManager<InquiryOrderVO>(() => ({
  BLTY: 'IQYORD',
  STFG: 'BL_D',
  STDT: dayjs().format('YYYY-MM-DD')
}))
formManager.setRules(formRules)

// ==================== 关系配置 ====================
formRelation.manager = formManager
formRelation.sqlQuery = (params) => ({
  loader: inquiryOrderLoader,
  params: Object.keys(params).reduce((pre, key) => {
    pre[`A.${key}`] = params[key]
    return pre
  }, {})
})
stdForm.relationRegister.register(formRelation)

// ==================== 数据加载器 ====================
const inquiryOrderLoader = async (params) => {
  const orderItem = await fetchInquiryOrder({ where: params })
  orderItem.FTYNO = orderItem.V_FTYNO  // 数据转换
  return orderItem
}

// ==================== 生命周期 ====================
stdForm.onInitDone(async () => {
  // 初始化完成后的逻辑
})

stdForm.onBeforeSubmit(async () => {
  // 提交前验证
  if (form.value.BEGFR && form.value.ENDTO) {
    const diff = dayjs(form.value.ENDTO).diff(dayjs(form.value.BEGFR), 'day')
    if (diff < 5) {
      ElMessageBox.alert('预计货期必须大于预计开机日期5个工作日以上！')
      return false
    }
  }
  return true
})

// ==================== 事件处理 ====================
const handleCLNTChange = (item) => {
  form.value.FTYNO = ''
  form.value.V_FTYNAME = ''
  form.value.CORPNO = item?.DEF_CORP
}
</script>
```

---

### 2.2 类型定义模板

```typescript
// types.ts
export type InquiryOrderVO = {
  PLNSEQ: number
  PLNNO: string       // 询单单号
  PLNMTH: string      // 询单年月
  BLTY: string        // 单类
  CORPNO: string      // 公司
  V_CORPNAME: string  // 公司名称
  CLNT: string        // 客户
  V_CLNTNAME: string  // 客户名称
  // ...
}
```

---

### 2.3 验证规则模板

```typescript
// validationRules.ts
import type { Rules } from 'async-validator'

export const formRules: Rules = {
  PLNSEQ: [{ required: true, message: 'PLNSEQ不能为空' }],
  PLNNO: [{ required: true, message: '询单单号不能为空' }],
  CORPNO: [{ required: true, message: '公司不能为空' }],
  CLNT: [{ required: true, message: '客户不能为空' }]
}
```

---

### 2.4 关系配置模板

```typescript
// relations/form.ts
import { ColumnType, KeyType, StdFormRelation } from '@/components/StdForm/types/stdForm'

export const formRelation: StdFormRelation<any> = {
  id: 'ORDPLN@APSDB169_ERPDB',
  table: {
    tableName: 'ORDPLN@APSDB169_ERPDB',
    columns: [
      { field: 'PLNSEQ', type: ColumnType.NUMBER, notNull: true, key: KeyType.PRIMARY },
      { field: 'PLNNO', description: '单号', notNull: true },
      { field: 'PLNMTH', type: ColumnType.NUMBER, description: '年月' },
      { field: 'BLTY', description: '单类', notNull: true, defaultValue: 'IQYORD' },
      // ...
    ]
  }
}
```

---

### 2.5 路由配置模板

```typescript
// router/index.js
export default [
  {
    path: '/inquiry-order',
    name: 'inquiry-order',
    component: () => import('../list.vue'),
    meta: {
      title: '交货计划',
      identity: 'VUE_INQUIRY'
    }
  },
  {
    path: '/inquiry-order-form',
    name: 'inquiry-order-form',
    component: () => import('../form/index.vue'),
    meta: {
      title: '交货计划',
      identity: 'VUE_INQUIRY'
    }
  }
]
```

---

## 三、列表页模板

```vue
<template>
  <div class="view-container w-full h-full box-border relative bg-white p-1 overflow-auto flex flex-col">
    <!-- 查询区域 -->
    <AceFieldset legend="查询" class="overflow-hidden min-w-[900px]">
      <FieldItemGroup :disabled="loading">
        <div>
          <FieldItem :label="t('lbPLNNO', '单号')" :width="32">
            <el-input class="w-[120px]!" v-model="query.PLNNO"/>
          </FieldItem>
          <FieldItem :width="0">
            <el-button type="primary" size="small" :loading="loading" @click="loadData">
              {{ c('common.search', '查询') }}
            </el-button>
            <el-button size="small" :loading="loading" @click="handleReset">
              {{ c('common.reset', '重置') }}
            </el-button>
          </FieldItem>
        </div>
      </FieldItemGroup>
    </AceFieldset>
    
    <!-- 数据区域 -->
    <div class="flex-1 flex flex-col" v-ace-loading="loading">
      <AceFieldset class="flex-1" legend="数据列表">
        <InquiryOrderTable 
          :data="dataList" 
          @open-order="handleOpenForm"
          @currentRowChange="handleCurrentRowChange"
        />
      </AceFieldset>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useParamsRefManager, useArrayRefManager } from '@/hooks/useRefManager'
import { useNsI18n } from '@/hooks/useNsI18n'
import AceFieldset from '@/components/AceFieldset/index.vue'
import InquiryOrderTable from './components/InquiryOrderTable.vue'

const { t, c } = useNsI18n('VUE_INQUIRY')
const loading = ref(false)

// 查询参数
const [query, queryManager] = useParamsRefManager(() => ({
  PLNNO: '',
  BEGFR: '',
  ENDTO: ''
}))

// 数据列表
const [dataList, dataListManager] = useArrayRefManager<InquiryOrderVO>([])

// 加载数据
const loadData = async () => {
  loading.value = true
  try {
    const params = query.value
    const list = await fetchInquiryOrderList({
      where: {
        'A.PLNNO__like': params.PLNNO
      }
    })
    dataListManager.update(list)
  } catch (err) {
    ElMessageBox.alert(`获取列表错误：${err.message}`)
  }
  loading.value = false
}

const handleReset = () => {
  queryManager.reset()
}

const handleOpenForm = (row?: InquiryOrderVO) => {
  // 打开表单
}
</script>
```

---

## 四、代码规范

### 4.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | 大驼峰 | `InquiryOrderTable.vue` |
| 页面文件 | 小驼峰 | `list.vue`, `index.vue` |
| 类型 | 大驼峰+VO后缀 | `InquiryOrderVO` |
| 函数 | 小驼峰+前缀 | `fetchInquiryOrderList` |
| 事件处理 | handle前缀 | `handleCLNTChange` |
| Ref变量 | Ref后缀 | `tableRef` |
| Manager | Manager后缀 | `formManager` |

### 4.2 导入顺序

```typescript
// 1. Vue核心
import { ref, computed, onMounted } from 'vue'

// 2. 第三方库
import dayjs from 'dayjs'

// 3. 组件
import StdFormWrapper from '@/components/StdForm/index.vue'

// 4. Hooks
import { useStdForm } from '@/components/StdForm/composeble/useStdForm'

// 5. 类型
import type { InquiryOrderVO } from './types'

// 6. 本地模块
import { formRelation } from './relations/form'
```

### 4.3 注释规范

```typescript
// ==================== 配置 ====================
// ==================== Hooks ====================
// ==================== 数据管理 ====================
// ==================== 生命周期 ====================
// ==================== 事件处理 ====================
```

---

## 五、常见问题

### 5.1 数据不更新

检查是否使用了正确的管理器方法：
```typescript
// ❌ 直接赋值不会触发更新
form.value = newData

// ✅ 使用管理器方法
formManager.update(newData)
```

### 5.2 表格数据不同步

确保使用 `records` 而非 `value`：
```typescript
// ✅ 正确
listManager.records  // 从表格组件获取数据

// ❌ 错误
listManager.value    // 原始响应式数组
```

### 5.3 验证不生效

确保设置了规则：
```typescript
formManager.setRules(formRules)
```

### 5.4 保存时数据缺失

检查关系配置中的 `columns` 是否包含所有需要的字段。