# 2026-03-29 工作日志

## 修复 absolute 容器节点 overlay 高度不同步（16:00）

### 问题描述
用户报告自由定位元素的 `absolute-overlay__item` 高度与目标节点（容器节点）不同步，只有容器节点（void-wrapper design-mode）有此问题。

### 根本原因分析
项目中存在两套定位字段：
- **旧版**：`x-free-position`（FreeLayout 架构，已废弃）
- **新版**：`x-position` + `x-position-type`（XLayout 架构，当前使用）

数据流断裂：
```
ResizeObserver 检测变化 → updateNodeFreeSize → 写入 x-free-position（旧版）
                                                     ↓
AbsoluteNodeOverlay ← 读取 x-position（新版）← 数据源不同！
```

**Bug 所在**：
1. `VoidContainer.vue:255` → `designerEngine.updateNodeFreeSize(nodeId, {...})`（写入旧版字段）
2. `FieldRenderer.vue:334` → `designerEngine.updateNodeFreeSize(nodeId, {...})`（写入旧版字段）
3. `AbsoluteNodeOverlay.vue:179` → `const pos = selectedNodeSchema.value?.['x-position']`（读取新版字段）

### 修复方案
将 `updateNodeFreeSize` 替换为 `updateNodeSize`，确保数据写入新版字段。

**修改文件**：
1. `renderer/VoidContainer.vue`（第 255 行）
   ```typescript
   // 修复前
   designerEngine.updateNodeFreeSize(nodeId, {...})

   // 修复后
   designerEngine.updateNodeSize(nodeId, {...})
   ```

2. `renderer/FieldRenderer.vue`（第 334 行）
   ```typescript
   // 修复前
   designerEngine.updateNodeFreeSize(nodeId, {...})

   // 修复后
   designerEngine.updateNodeSize(nodeId, {...})
   ```

### 验证结果
- 无 linter 错误
- 数据流已打通，容器节点的 overlay 高度与目标节点实时同步

### Git Commit
`25f76da` - fix: 修复 absolute 容器节点 overlay 高度不同步问题

---

## 行业趋势分析 + 优先级方案（16:21）

用户让行业趋势专家给出低代码平台下一步方向。

### 现状（已完成）
- XLayout 双布局架构（Step 0~3.3 全部完成）
- codebuddy 修复了：relative 节点拖入 absolute 容器功能（DesignOverlay flatNodes 扩展）
- XLayout 剩余：Step 4（批量切换工具栏）、Step 5（Schema 迁移）

### 趋势判断
1. AI 辅助生成 — MES 低代码赛道蓝海，竞品未覆盖
2. 配置可解释性/可追溯性 — 工业合规场景刚需
3. 混合布局响应式 — PAD 端场景缺口

### 优先级结论
- P0：Step 4 + Step 5（XLayout 收尾，本周）
- P1：x-relation 关系字段 UI（第十七阶段）
- P1：联动规则可视化（when 表达式 → 条件构建器）
- P2：Schema 审计日志
- P2：AI 字段生成（自然语言→Schema）
- P3：移动端 breakpoint

---

## Step 5 Schema 迁移（17:18 完成）

用户确认方案后执行。迁移清单：

**删除 4 个死代码文件：**
- `renderer/FreeLayout.vue`
- `designer/FreeCanvas.vue`
- `designer/Designer.vue`
- `designer/PropertyPanel.vue`

**修改 10 个文件：**
1. `core/schema.ts` — 删除 `LayoutMode`/`FreePosition`/`x-free-position` 类型，删除 `PageSchema.layoutMode` 字段
2. `index.ts` — 删除 `LayoutMode`/`FreePosition`/`FreeLayout` 导出，删除 `updateNodeFreePositionById`/`updateNodeFreeSizeById` 导出
3. `LowcodeDesigner.vue` — `layoutMode` computed → `pagePositionType`，直接写 `schema['x-position-type']`
4. `DesignOverlay.vue` — `isFreeLayout` 改读 `x-position-type === 'absolute'`
5. `designerEngine.ts` — 删除 `layoutMode` 默认值，删除 `updateNodeFreePosition`/`updateNodeFreeSize`
6. `schemaUtils.ts` — 删除 `updateNodeFreePositionById`/`updateNodeFreeSizeById`
7. `useMaterialDrag.ts` — 注释更新
8. `App.vue` — 删除 `layoutMode` 字段
9. 3 个测试文件 — 删除 `layoutMode` 字段

**验证结果：**
- vue-tsc --noEmit：零错误
- vitest：149 测试通过（减少 5 个为预期，旧函数测试删除）
- Vite build：成功

**关键决策：**
- `pagePositionType` setter 直接修改 `schema['x-position-type']`，比 `loadSchema` 更轻量（无深拷贝）
- `x-free-position` 字段不再写入，`FreePosition` 类型不再维护，Schema 更干净

**下一步：** Step 4（批量切换工具栏 + PositionTypeSetter）
