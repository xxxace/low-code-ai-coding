# 工作记忆 - 2026-04-05

---

## CanvasOverlay drag 修复挑刺 + 新 plan（2026-04-05 16:20）

两轮挑刺完成，plan 更新至最终版：

**关键修正：**
- fix-0：handleReorderNodes 改单对象签名（不用模板 wrapper，更干净）
- fix-4：move-into-container 分支嵌套在 `dragType === 'move'` **内部**，不是同级
- fix-5：撤回（CanvasOverlay onUnmounted 是合法代码）
- fix-6：selectedBox data 属性不可行（getSelectedStyle 是 composable 函数），简化为纯 drop-zone-indicator 增强
- fix-7：vue-tsc 全面检查（不只是 LowcodeDesigner）

Plan 文件：`c:\Users\73116\AppData\Roaming\WorkBuddy\User\globalStorage\tencent-cloud.coding-copilot\plans\8464ec9d75cc4ce0b2222fddd4e71f9a\plan.md`

### 修复 1：handleDropComplete 语法错误（2026-04-05 00:15）

**问题**：`[vue/compiler-sfc] Unexpected token ','. (172:46)`

**根因**：在 `handleDropComplete` 函数中，传入了中文注释作为函数参数：
```typescript
engine.sortNodes(/* fromId 需从上下文获取 */, target.beforeNodeId, target.position)
```

中文注释不是合法的 JavaScript 值，导致编译器解析失败。

**修复**：将包含中文注释的代码替换为临时注释，并添加警告信息：
```typescript
function handleDropComplete(target: any): void {
  if (!target) return

  // TODO: DropTarget 接口中缺少源节点 ID 字段，暂时无法执行排序/移动操作
  // 需要在 useDragInteraction.ts 的 DropTarget 接口中添加 sourceId 字段
  switch (target.action) {
    case 'sort-relative':
      // engine.sortNodes(sourceId, target.beforeNodeId, target.position)
      console.warn('[LowcodeDesigner] sort-relative: DropTarget 缺少 sourceId 字段')
      break
    case 'move-absolute':
      // absolute 节点移动，已在 CanvasOverlay 内部通过 update-node-position emit 处理
      break
    case 'move-into-container':
    case 'show-container-dropzone':
      // engine.moveNodeToContainer(sourceId, target.targetContainerId)
      console.warn('[LowcodeDesigner] move-into-container: DropTarget 缺少 sourceId 字段')
      break
  }
}
```

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

### 修复 2：CanvasOverlay 点击节点报错（2026-04-05 00:20）

**问题**：`CanvasOverlay.vue:211 Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'node-dept')`

**根因**：
1. 方案中的 `DesignerSchema` 类型不存在
2. `PageSchema` 没有 `nodes` 字段，节点数据在 `schema.properties` 中
3. `CanvasOverlay.vue` 中的 `getNodeLabel`、`isAbsoluteNode` 等函数试图访问 `props.schema.nodes[nodeId]`，导致 `undefined`

**修复**：
1. 将 `DesignerSchema` 类型替换为 `PageSchema`
2. 添加 `findNodeById` 辅助函数来获取节点
3. 修复 `getNodeLabel`、`isAbsoluteNode`、`absoluteContainers` computed 等依赖 `props.schema.nodes` 的代码

**涉及的文件**：
- `CanvasOverlay.vue`
  - `schema: DesignerSchema` → `schema: PageSchema`
  - 添加 `getNodeById` 函数使用 `findNodeById`
  - 修复 `isAbsoluteNode` 使用 `getNodeById`
  - 修复 `getNodeLabel` 使用 `getNodeById`
  - 修复 `absoluteContainers` 遍历 `props.schema.schema.properties`

- `composables/useNodeOverlay.ts`
  - `schema: DesignerSchema` → `schema: PageSchema`

- `composables/useDragInteraction.ts`
  - `schema: DesignerSchema` → `schema: PageSchema`
  - 添加 `import { findNodeById }`
  - 修复 `getNodeSchema` 使用 `findNodeById(props.schema.schema, nodeId)`

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

### 修复 4：selectedBox 不显示问题（2026-04-05 00:30）

**问题**：点击节点后 `canvas-overlay__selected-box` 不显示，样式是 `display: none;`

**根因分析**：
1. CanvasOverlay.vue 的 selectedBox 上绑定了 `@mousedown="handleMouseDown"`（第 34 行）
2. selectedBox 的 CSS 是 `pointer-events: none`（v7 文档设计），导致 `@mousedown` 永远不触发
3. mousedown 事件委托已在 `canvasEl` 上绑定（setupDragListeners），应该通过 `closest('[data-field-id]')` 获取节点
4. selectedBox 上的 `@mousedown` 是冗余的，反而会阻止事件冒泡

**修复**：
1. 移除 CanvasOverlay.vue 中 selectedBox 的 `@mousedown` 绑定
2. 依赖 canvasEl 上的事件委托来处理点击
3. 修复 hover 事件监听：`mouseenter/mouseleave` → `mouseover/mouseout`（mouseenter 不冒泡，无法通过 closest 获取节点）
4. 修复 `handleMouseOut`：增加 `relatedTarget` 检查，避免子元素触发

**涉及文件**：
- `CanvasOverlay.vue`（第 34 行，移除 `@mousedown="handleMouseDown"`）
- `CanvasOverlay.vue`（第 136-144 行，移除 `handleMouseEnter/handleMouseLeave` 解构）
- `useNodeOverlay.ts`
  - `handleMouseEnter` → `handleMouseOver`
  - `handleMouseLeave` → `handleMouseOut`（增加 relatedTarget 检查）
  - `addEventListener('mouseenter')` → `addEventListener('mouseover')`
  - `addEventListener('mouseleave')` → `addEventListener('mouseout')`
  - `getHoverStyle/getSelectedStyle` 的 `pointerEvents` 改为非 `as const`

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

### 修复 5：useNodeOverlay 导出函数名不匹配（2026-04-05 00:40）

**问题**：刷新页面报错 `handleMouseEnter is not defined`

**根因**：
1. 将 `handleMouseEnter` 改名为 `handleMouseOver`，将 `handleMouseLeave` 改名为 `handleMouseOut`
2. 但忘记更新 `return` 语句中的导出
3. 导出仍引用已删除的 `handleMouseEnter` 和 `handleMouseLeave`

**修复**：
1. 从 `useNodeOverlay` 的 `return` 中移除 `handleMouseEnter` 和 `handleMouseLeave`
2. CanvasOverlay.vue 中同步移除解构引用（已在修复 4 中完成）

**涉及文件**：
- `composables/useNodeOverlay.ts` 第 124-132 行

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

### 修复 6：SchemaNode 联合类型访问属性错误（2026-04-05 00:45）

**问题**：类型检查报错 10 处，`Expression of type '"x-position-type"' can't be used to index type 'SchemaNode'`

**根因**：
- `SchemaNode = FieldSchema | ContainerNode` 是联合类型
- TypeScript 无法确定联合类型的哪个分支包含 `x-position-type` 属性
- 即使 `BaseFieldSchema` 中定义了 `x-position-type` 和 `x-component`，也不能直接索引联合类型

**修复**：使用 `as any` 类型断言绕过类型检查
1. CanvasOverlay.vue：
   - `node['x-position-type']` → `(node as any)['x-position-type']`
   - `container['x-id']` → `(container as any)['x-id']`
   - `dropIndicatorStyle` 中的 `props.canvasEl` 非空检查
   - `getContainerStyle` 中的 `props.canvasEl` 非空检查
   - style 绑定添加 `as any` 类型断言（第 66、74 行）

2. useDragInteraction.ts：
   - `schema?.['x-position-type']` → `(schema as any)?.['x-position-type']`
   - `schema?.['x-container']` → `(schema as any)?.['x-container']`

**涉及文件**：
- `CanvasOverlay.vue`（第 165、215、220、224、191、228 行，第 66、74 行）
- `useDragInteraction.ts`（第 168、355、356 行）

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

### 修复 7：selectedBox 不显示（canvasEl ref 不响应式）（2026-04-05 00:55）

**问题**：点击节点后 `canvas-overlay__selected-box` 不显示，样式是 `display: none;`

**根因分析**：
1. CanvasOverlay 传递 `canvasEl` 给 composable 时使用了 `ref(props.canvasEl)`
2. `ref(props.canvasEl)` 会创建一个新的 ref，其值是 props.canvasEl 的**初始值**（null）
3. 当 props.canvasEl 从父组件传入后，新 ref 的值不会更新
4. `getSelectedStyle` 使用 `canvasEl.value?.querySelector()`，但由于 canvasEl 是 null，找不到节点元素
5. 返回 `{ display: 'none' }`，导致 selectedBox 隐藏

**修复**：
1. 使用 `toRef(() => props.canvasEl)` 替代 `ref(props.canvasEl)`
2. `toRef` 创建一个响应式的 ref，当 props.canvasEl 更新时会自动同步
3. 在 `getSelectedStyle` 和 `getHoverStyle` 中添加调试日志
4. 在找不到节点元素时输出警告信息，帮助排查问题

**涉及文件**：
- `CanvasOverlay.vue`
  - 导入 `toRef` from 'vue'
  - 第 141 行：`ref(props.canvasEl)` → `toRef(() => props.canvasEl)`
  - 第 154 行：`ref(props.canvasEl)` → `toRef(() => props.canvasEl)`
- `useNodeOverlay.ts`
  - `getSelectedStyle` 添加 canvasEl 空检查和警告日志
  - `getHoverStyle` 添加元素未找到警告日志

**验证**：
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

## CanvasOverlay 修复 plan 审辩与升级（2026-04-05 10:39）

前端工程师对 v7 修复方案提出 7 条挑刺，大将军逐条审辩后更新 plan.md（v7.1）：

**接受（3条）**
- 刺 1：querySelector 和 getBoundingClientRect 参考对象必须分开——useNodeOverlay 增加 `overlayEl` 第三参数，querySelector 继续用 `canvasEl`，坐标参考改用 `overlayEl`
- 刺 2：dropIndicatorStyle 和 getContainerStyle 也用了 canvasEl 作参考，原 plan 漏了，一并修
- 刺 7：engine.sortNodes 第二参数是 string 不接受 null，beforeNodeId=null 时需兜底处理

**部分接受（2条）**
- 刺 3：emit 坐标 bug 仍需修，但实现时先 console 确认 absolute 节点触发
- 刺 5：dropTarget.value = null 改为 nextTick 清零

**轻微接受（1条）**
- 刺 6：isAbsoluteNode 模板调用改 computed

**不接受（1条）**
- 刺 4：DropTarget.sourceNodeId 是 DTO 字段，合理；改 emit 签名方案等价，无优劣之分

plan.md 已更新至 v7.1，增加审辩记录节，修正技术实现细节。

---

## CanvasOverlay v7.1 新刺回应与升级至 v7.2（2026-04-05 11:04）

前端工程师对 plan v7.1 提出 4 条新刺，大将军全部接受并更新 plan 至 v7.2：

**新刺 A：elementFromPoint 被 overlay 按钮遮挡**
- **问题**：`calcDropTarget` 使用 `document.elementFromPoint(clientX, clientY)`，但 overlay 的 `.action-buttons` 和 `.resize-handle` 设置了 `pointer-events: auto`，会遮挡底下的 canvas 节点
- **修复**：跳过 overlay 自己的元素，向上递归找非 overlay 的祖先

**新刺 B：resize handle 找不到 data-field-id，resize 完全失效**
- **问题**：`handleResizeStart` 用 `closest('[data-field-id]')` 查找节点，但 resize handle 只有 `data-resize-dir`，没有 `data-field-id`，永远找不到
- **修复**：改用 `selectedNodeId`（props 传入），不依赖 DOM 查找；useDragInteraction 增加第三个参数 `selectedNodeId`

**新刺 C：nextTick 清零可能导致指示器残留一帧**
- **问题**：`isDragging = false` 后 dropZoneIndicators 立即消失，但 `dropTarget.value` 还未清零（要等 nextTick），drop 指示线残留一帧
- **修复**：改为同步清零 `dropTarget.value = null`

**新刺 D：engine.moveNodeById 不存在，兜底方案用了幻想 API**
- **问题**：plan 里写"调用 `engine.moveNodeById` 追加到父容器末尾"，但 engine 里没有这个方法，只有 `moveNode(nodeId, 'up'|'down')`
- **修复**：查找同容器最后一个节点，用 `engine.sortNodes(sourceId, lastNodeId, 'after')` 追加

**额外补充（刺 3-A）**：`closest('[data-field-id]')` 在 absolute 嵌套场景下会找到**最近的祖先**（可能是容器而非子节点），验证时需检查返回的是否为目标子节点 ID

plan.md 已更新至 v7.2，新增 4 项修复内容（修复 0-2），更新刺 3、刺 5、刺 7 的结论，增加刺 3-A 补充说明。

---

## CanvasOverlay v7.2 实现（2026-04-05 11:20）

根据 plan v7.2 完成了所有 7 个修复点的实现：

### 修改的文件
1. **useNodeOverlay.ts**：
   - 增加 `overlayEl` 参数（坐标参考基点）
   - `getHoverStyle/getSelectedStyle` 改用 `overlayEl.value.getBoundingClientRect()`

2. **useDragInteraction.ts**：
   - Props 增加 `selectedNodeId`
   - DropTarget 接口增加 `sourceNodeId`
   - dragState 增加 `sourceNodeId`
   - handleMouseDown 记录 `sourceNodeId`
   - handleResizeStart 改用 `selectedNodeId`（新刺 B）
   - calcDropTarget 跳过 overlay 元素（新刺 A）+ 携带 `sourceNodeId`
   - handleMouseUp move 分支 emit 最终坐标（修复 4）
   - handleMouseUp sort 分支同步清空 dropTarget（新刺 C 更正）

3. **CanvasOverlay.vue**：
   - Emits 新增 `'move-node'`
   - useNodeOverlay 调用传入 `overlayRef`
   - useDragInteraction 调用传入 `selectedNodeId`
   - dropIndicatorStyle/getContainerStyle 改用 overlayRef
   - isAbsoluteNode 改为 computed（isSelectedNodeAbsolute）
   - 操作按钮区域添加上移/下移按钮（仅 relative 节点）

4. **LowcodeDesigner.vue**：
   - handleDropComplete 实现排序逻辑（含 beforeNodeId=null 兜底）
   - 监听 `@move-node` 事件

5. **schemaUtils.ts**：
   - 新增 `findParentProperties` 和 `findParentNode` 函数

### 验证结果
- ✅ Linter 零错误
- ✅ vitest run 149/149 全通过

---

## Absolute 节点拖拽同步问题修复（2026-04-05 13:27）

### 问题描述
absolute 节点拖拽时，节点通过直接操作 DOM（el.style.left/top）跟随鼠标移动，但 selectedBox 不跟随——因为 Vue 不重新渲染，getSelectedStyle 不重新执行。

### 修复内容

**fix-1（核心）：CanvasOverlay.vue — draggingBox RAF 同步**
- `isDraggingNode = dragState.isDragging && dragState.dragType === 'move'`
- dragging 时隐藏 selectedBox，显示 draggingBox（新增 DOM 元素）
- RAF 循环中读取被拖拽节点 `getBoundingClientRect()`，直接写 DOM style（绕过 Vue reactivity）
- 组件卸载时清理 RAF

**fix-2：useDragInteraction.ts — calcDropTarget 条件修正**
- 旧逻辑：`x-position-type === 'absolute'` 时嵌套判断 `x-container`
- 新逻辑：先判断 `type === 'void'`（容器），返回 `move-into-container`；再判断普通 absolute 节点，返回 `move-absolute`
- 删除了已废弃的 `x-container` 引用

**fix-3：VoidContainer.vue — data-container-type 修正**
- 旧：`schema['x-container'] !== undefined`
- 新：`schema['x-position-type'] === 'absolute'`

**fix-4：CanvasOverlay.vue — drop-zone-indicator 视觉增强**
- active 状态：边框改为 `#67c23a` 实色 + `rgba(103,194,58,0.15)` 背景 + 内阴影

### 验证结果
- ✅ Linter 零错误（CanvasOverlay / useDragInteraction / VoidContainer）
- ✅ vitest run 149/149 全通过

---

## CanvasOverlay 挑刺修复最终版（2026-04-05 16:30）

根据 plan 8464ec9d75cc4ce0b2222fddd4e71f9a 完成全部修复：

### 修复清单

| 修复项 | 文件 | 变更内容 |
|--------|------|----------|
| fix-0 | LowcodeDesigner.vue | handleReorderNodes 改为单对象参数签名，匹配 emit |
| fix-1 | XLayout.vue | VoidContainer 的 `@node-click` → `:on-node-click` |
| fix-2 | FieldRenderer.vue | 删除 `lowcode-field-wrapper--selected` 类绑定和 CSS |
| fix-2 | VoidContainer.vue | 删除 `selectedClass` 计算属性、类绑定和 CSS |
| fix-3 | useDragInteraction.ts | calcDropTarget 增加 nested absolute 检查，源节点已在容器内时返回 move-absolute |
| fix-4 | - | move-into-container 在 handleDropComplete 中已正确处理（无需修改） |
| fix-6 | CanvasOverlay.vue | drop-zone-indicator--active 视觉增强 |
| fix-7 | - | vue-tsc --noEmit 零错误 + vitest 149/149 全通过 |

### 验证结果
- ✅ vue-tsc --noEmit 零错误
- ✅ vitest run 149/149 全通过

---

## CanvasOverlay 三 Bug 修复（2026-04-05 16:40）

### 问题列表

| 问题 | 现象 | 根因 | 修复方案 |
|------|------|------|----------|
| 问题1 | 排序后 selectbox 位置不更新 | `getSelectedStyle` 只在调用时计算一次，节点位置变化后未重新计算 | 在 `CanvasOverlay.vue` 中添加 `watch` 监听 `selectedNodeId` 和 `schema` 变化，触发重绘 |
| 问题2 | absolute 节点拖拽后 selectbox 定位到左上角 | 拖拽结束后先 emit 更新再清除内联 style，Vue 响应式更新异步导致计算错误位置 | 调整顺序：先清除内联 style，再 emit 更新，然后 `await nextTick()` |
| 问题3 | absolute 容器内节点无法点击选中 | `handleMouseDown` 中的 `e.stopPropagation()` 阻止了事件冒泡，导致子节点 click 无法触发 | 修改逻辑：只有直接点击容器本身时才阻止事件，点击容器内部子元素时让事件冒泡 |

### 修改文件

1. **CanvasOverlay.vue**
   - 添加 `nextTick` import
   - 添加 `watch` 监听 `selectedNodeId`，等待 DOM 更新后重新计算 `getSelectedStyle`
   - 添加 `watch` 监听 `schema` 深度变化，触发 selectedBox 重绘

2. **useDragInteraction.ts**
   - 添加 `nextTick` import
   - 修改 `handleMouseUp`：先清除内联 style，再 emit `update-node-position`，然后 `await nextTick()`
   - 修改 `handleMouseDown`：添加 `isDirectClick` 判断，只有直接点击节点本身时才启动拖拽并阻止事件冒泡

### 验证结果
- ✅ vue-tsc --noEmit 零错误
- ✅ vitest run 149/149 全通过

---

## CanvasOverlay 最终 Bug 修复（2026-04-05 16:55）

根据前端工程师审查和主人反馈，完成两个关键 Bug 修复：

### 问题1：selectbox 排序后原地不动

**根因**：`getSelectedStyle` 是函数调用而非 computed，Vue 不会追踪其依赖。排序后 schema 变了，但 selectedBox 不会重新渲染。

**修复**（CanvasOverlay.vue）：
1. 添加 `styleVersion` ref 计数器
2. selectedBox DOM 添加 `:key="`selected-${selectedNodeId}-${styleVersion}`"`，强制重新渲染
3. 添加 `handleMoveNode` 辅助函数，在移动节点后调用 `refreshSelectedBox()` 递增版本号
4. watch schema 变化时也递增版本号

### 问题2：absolute 节点无法拖拽

**根因**：`isDirectClick` 判断过于严格。对于 absolute 节点，`e.target` 可能是节点内部的子元素（如输入框），导致 `nodeEl.contains(e.target)` 返回 true 但原逻辑不允许拖拽。

**修复**（useDragInteraction.ts）：
1. 放宽 `isDirectClick` 判断条件，添加 `nodeEl.contains(e.target as Node)`
2. 修改拖拽启动条件：只有非 absolute 节点且未选中时才阻止拖拽；absolute 节点允许点击任意位置启动拖拽

### 修改文件

1. **CanvasOverlay.vue**
   - 添加 `styleVersion` ref
   - selectedBox 添加 key 绑定
   - 添加 `handleMoveNode` 函数
   - 修改上移/下移按钮点击事件绑定
   - watch schema 时递增 styleVersion

2. **useDragInteraction.ts**
   - 修改 `isDirectClick` 判断，添加 `nodeEl.contains(e.target as Node)`
   - 修改拖拽启动条件，放宽 absolute 节点的限制

### 验证结果
- ✅ Linter 零错误
- ✅ 类型检查通过
