# 2026-03-27 工作日志

## 联动规则编辑器三区布局重构（第二次）

### 背景
多条规则时用户找不到目标规则，注意力分散，需要主从布局。

### 改动
- **ReactionEditorDialog 全量重写**：三区布局
  - 顶栏：搜索框（按名称/条件过滤）+ 新增按钮
  - 左侧（210px）：规则名称列表 + 颜色徽章（显示/隐藏/禁用/只读/必填）+ 条件预览
  - 右侧：详情编辑（名称可编辑 + 条件/依赖/目标/效果 + 备注多行文本）
  - 新增规则时自动选中；删除后自动切换到相邻规则
- **schema.ts Reaction 接口**：新增 `name?: string` 和 `remark?: string`（设计器 UI 专用字段）

## 规则启用/禁用机制（三技能重构）

- `Reaction` 接口加 `enabled?: boolean`（默认 true = 启用）
- `reactions.ts._executeReaction` 头部加 `if (reaction.enabled === false) return`，引擎跳过禁用规则
- `ReactionEditorDialog` 左侧徽章语义变更：绿=规则已启用 / 灰=规则已禁用（`rd-badge--enabled` / `rd-badge--disabled-rule`），禁用行整体 opacity 0.5
- 右侧详情顶部新增启用/禁用 toggle 按钮（el-button success/info plain），点击调用 `toggleEnabled(idx)` 切换 enabled
- 效果类型精简：去掉"只读(readOnly)"，保留 显示/隐藏/禁用字段/必填/取消必填；`parseFulfillType` 和 `updateFulfill` 均同步调整
- `FieldProperties.reactionFulfillLabel`：禁用规则显示"（已禁用）"，`disabled` 字段替换 `pattern` 分支
- 属性面板摘要卡片：禁用规则加 `.reaction-chip--disabled` 半透明样式


## 功能清单归档（2026-03-27）

- 创建 `D:\demo\ai\aiSpace\FEATURE_CHECKLIST.md`：含已完成/骨架在/待实现/已知Bug/ADR/目录结构六大板块
- 在 MEMORY.md 顶部增加指引，后续 session 开始时可直接读取该文件获知全貌

## designer/ 目录清理（2026-03-27 上午）

- `Designer.vue` import 路径从 `./designerEngine` 迁移到 `./engine/designerEngine`
- 删除 4 个废弃文件：`designerEngine.ts`（旧根目录版）、`useDragSort.ts`、`useFreeDrag.ts`、`useResize.ts`
- 零引用确认后删除，linter 零错误

## 第十四阶段完成：架构重构（core/ 分层 + 垫片兼容）

### 完成内容
- `FieldRenderer.vue`：添加 ContainerNode 占位分支（`type='container'` 走独立渲染路径，slot 供嵌套，`.lowcode-container-placeholder` 样式）
- `src/index.ts`：全量重写，从 `types/` 改为从 `core/` 导出；新增 `I18nString`、`ContainerNode`、`SchemaNode`、`DataSourceConfig`、`FunctionRegistry`、`designerBus`、`schemaUtils` 8个纯函数的导出

### 验证结果
- `tsc --noEmit`：零错误
- `vite build`：✅ 成功，1663 模块，无报错（仅 chunk size 警告，正常）

### 问题分析
- **问题1**：Schema 中联动规则可以用 `$self.value` 自引用（无 dependencies），但界面"依赖"字段为空，信息对不上——用户困惑
- **问题2**：联动规则块在属性面板占用大量空间，后续属性会越来越多

### 解决方案
**第一性原理**：联动本质 = 表达式（when 字符串）+ 效果（fulfill/otherwise），不必区分 $self 与 $deps，直接编辑 when 字符串即可。
**奥卡姆剃刀**：属性面板只显示摘要（N 条规则 + 每条一行 when→fulfill），编辑全在弹窗。

### 改动
- **新建 `ReactionEditorDialog.vue`**：联动规则完整编辑弹窗
  - 变量说明区（$self.value / $deps[0] / $values.xxx）
  - 直接编辑 `when` 条件表达式字符串
  - 依赖字段（仅用 $deps 时填）
  - 目标字段（target，空=作用自身）
  - 满足时效果选择 + 不满足时自动推导展示
- **重构 `FieldProperties.vue` 联动规则区**：
  - 删除原来的内联编辑 UI（reactionDepsInput/reactionFulfillType 等）
  - 改为「共 N 条规则」摘要 + 「编辑」按钮 + 每条规则一行摘要卡片（when → 显示/隐藏/...）
  - 引入 `ReactionEditorDialog`，通过 `@confirm` 回写 reactions

## TypeScript 类型清理（2026-03-27）

`tsc --noEmit` 零错误通过，解决 13 个类型报错：
- 创建 `src/vite-env.d.ts`：Vite 环境变量类型声明
- 修复 `src/core/schema.ts`：ObjectFieldSchema 使用 `Omit<BaseFieldSchema, 'required'>` 解决 required 语义冲突
- 创建 `src/vue-shim.d.ts`：Vue SFC 模块类型声明

### TypeScript 类型清理完整版（vue-tsc）（2026-03-27）

22 个 `vue-tsc` 错误全部解决（上次 tsc 零错误是误判，vue-tsc 才是正确的检查工具）：

| 文件 | 修复 |
|------|------|
| `vite-env.d.ts` | 新增 element-plus locale 模块类型声明 |
| `schema.ts` | 新增 `AllFieldTypes` 联合别名（解决 FieldProperties 更新对象类型推断问题） |
| `FieldProperties.vue` | `Partial<FieldSchema>` 类型标注改为对象尾部 `as Partial<FieldSchema>` 断言 |
| `FreeCanvas.vue` | emit 参数 `?? ''` 防止 null；移除未使用的 `canvasEl` prop |
| `Designer.vue` | FreeCanvas 改回 `:schema` + `!` 非空断言；直接调用解构的 `duplicateNode/removeNode` |
| `LowcodeDesigner.vue` | `@move-node` 从 `$event[0], $event[1]` 改为 `([nodeId, dir]) =>` 解构 |
| `FieldRenderer.vue` | `FormItemRule` 类型导入；`computed<FormItemRule[]>` 明确返回类型；`??` 改 `||` |
| `FormRenderer.vue` | `disabled.value` 改为模板自动解包的 `disabled` |
| `FlowLayout.vue` / `FreeLayout.vue` | `FormModelInstance = InstanceType<typeof FormModel>` 类型别名解决 class Ref 属性解包问题 |
| `FreeLayout.vue` | `properties[nodeId]` → `props.properties[nodeId]` |
| `VoidContainer.vue` | `schema.properties` → `schema?.properties`；子级属性加 `(schema as any).properties` |
| `PropertyPanel.vue` | `localSchema.value[path]` → `(localSchema.value as Record<string,unknown>)[path]` |

### 第二轮漏网之鱼（vue-tsc 又发现 8 个新错误）

**根因**：vue-tsc 对 Vue 模板的类型推断规则与 `tsc` 不同。

| 文件 | 修复 |
|------|------|
| `Designer.vue` | `duplicateNode`/`removeNode` 未从 `useDesignerEngine()` 解构 → 添加到解构列表 |
| `Designer.vue` | FreeCanvas `:canvas-el` 移除（prop 已删除）；`:schema="schema!"` 改回 `:schema="schema!"` |
| `FreeLayout.vue` | emit 定义正确，但 vue-tsc overload 匹配失败 → 添加 `emitAs: EmitType` 类型别名绕过严格检查 |
| `FreeLayout.vue` | `emit('select-node', ...)` → `emitAs('select-node', ...)`（4处） |
| `FormRenderer.vue` | 模板中 `:form-model="formModel"`（`ref<T>`）推断为 `UnwrapRef<T>`，与子组件 prop `FormModel \| null` 不匹配 → 添加 `formModelForTemplate = computed(...)` 中转属性 + `as FormModel \| null` 断言 |
| `FlowLayout.vue` | `formModel` prop 类型改为 `FormModel \| null` |
| `FreeLayout.vue` | `formModel` prop 类型改为 `FormModel \| null` |
| `VoidContainer.vue` | `formModel` prop 类型改为 `FormModel \| null` |

