# 下一阶段任务优先级建议

**生成时间**: 2026-03-26 11:25  
**建议者**: nanobot 🦀

---

## 候选任务列表

来源：workbuddy MEMORY.md + QA 报告分析

1. FreeLayout 拖拽移动 + 8方向缩放
2. StdForm 适配层（RelationRegister / useArrayRefManager 对接）
3. 单元测试（ReactionsEngine、FormModel）
4. ComponentRegistry 懒加载支持
5. ValidatorRegistry 机制实现
6. 吸附检测修复

---

## 优先级排序

### 🔴 P1 - 高优先级（本周完成）

#### 1. FreeLayout 拖拽移动 + 8方向缩放

**优先级理由**:
- **用户可见价值**: ⭐⭐⭐⭐⭐ 设计器核心交互，用户直接感知
- **依赖关系**: 是吸附检测的前置条件
- **代码完整度**: `useFreeDrag.ts` 和 `useResize.ts` 已有骨架，只需集成
- **风险**: 低，纯前端逻辑

**预估工时**: 4-6 小时

**建议实现顺序**:
1. 在 `FreeLayout.vue` 中集成 `useFreeDrag` 和 `useResize`
2. 处理 `x-free-position` 的响应式更新
3. 添加吸附线 UI 显示

---

#### 2. 吸附检测修复

**优先级理由**:
- **用户可见价值**: ⭐⭐⭐⭐ 自由布局体验关键
- **依赖关系**: 依赖任务 1 完成
- **代码完整度**: 逻辑已存在，只需修复数据传递
- **风险**: 低

**预估工时**: 2-3 小时

**修复要点**:
- `FreeCanvas.vue` 中调用 `updateOtherPositions`
- 在 `watch` 或 `onMounted` 中同步其他节点位置

---

### 🟡 P2 - 中优先级（下周完成）

#### 3. StdForm 适配层

**优先级理由**:
- **用户可见价值**: ⭐⭐⭐⭐ 打通现有 StdForm 系统，实现业务价值
- **依赖关系**: 独立任务，无阻塞
- **代码完整度**: 需要新建适配层代码
- **风险**: 中，需要理解现有 StdForm 架构

**预估工时**: 8-12 小时

**建议实现内容**:
1. `StdFormAdapter.vue` - 对接 `useStdForm` 的生命周期
2. `RelationFieldRenderer.vue` - 渲染 `x-relation` 配置的子表格
3. 数据转换层：PageSchema ↔ StdForm config

---

#### 4. 单元测试（ReactionsEngine、FormModel）

**优先级理由**:
- **用户可见价值**: ⭐⭐⭐ 降低回归风险，提升代码信心
- **依赖关系**: 独立任务
- **代码完整度**: 核心逻辑已稳定，适合编写测试
- **风险**: 低

**预估工时**: 6-8 小时

**建议测试覆盖**:
- `FormModel`: setFieldValue, validate, reset, getSubmitValues
- `ReactionsEngine`: 依赖收集、条件求值、副作用执行
- `HistoryManager`: undo/redo 快照管理

---

### 🟢 P3 - 低优先级（后续迭代）

#### 5. ValidatorRegistry 机制实现

**优先级理由**:
- **用户可见价值**: ⭐⭐ 支持自定义校验，非核心功能
- **依赖关系**: 独立任务
- **代码完整度**: 类型已定义，需要实现注册逻辑
- **风险**: 低

**预估工时**: 4-6 小时

---

#### 6. ComponentRegistry 懒加载支持

**优先级理由**:
- **用户可见价值**: ⭐⭐ 首屏性能优化
- **依赖关系**: 独立任务
- **代码完整度**: 当前注册表已可用
- **风险**: 低

**预估工时**: 3-4 小时

---

## 任务依赖图

```
┌─────────────────────────────────────────────────────────┐
│                     Week 1 (P1)                         │
│  ┌───────────────────┐    ┌───────────────────┐        │
│  │ FreeLayout 拖拽   │───→│   吸附检测修复    │        │
│  │ + 8方向缩放       │    │                   │        │
│  └───────────────────┘    └───────────────────┘        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                     Week 2 (P2)                         │
│  ┌───────────────────┐    ┌───────────────────┐        │
│  │  StdForm 适配层   │    │    单元测试       │        │
│  │                   │    │                   │        │
│  └───────────────────┘    └───────────────────┘        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   迭代中 (P3)                           │
│  ┌───────────────────┐    ┌───────────────────┐        │
│  │ ValidatorRegistry │    │ ComponentRegistry │        │
│  │                   │    │   懒加载          │        │
│  └───────────────────┘    └───────────────────┘        │
└─────────────────────────────────────────────────────────┘
```

---

## 风险评估

| 任务 | 技术风险 | 业务风险 | 建议 |
|------|----------|----------|------|
| FreeLayout 拖拽 | 低 | 低 | 直接实现 |
| 吸附检测修复 | 低 | 低 | 直接修复 |
| StdForm 适配层 | 中 | 中 | 先做技术预研 |
| 单元测试 | 低 | 低 | 逐步覆盖 |
| ValidatorRegistry | 低 | 低 | 按需实现 |
| ComponentRegistry 懒加载 | 低 | 低 | 性能优化时处理 |

---

## 总结

**本周重点**: FreeLayout 拖拽/缩放 + 吸附检测修复

**理由**:
1. 这是设计器最核心的用户交互功能
2. 代码骨架已存在，修复成本低
3. 完成后设计器即可达到可用状态

**下周重点**: StdForm 适配层 + 单元测试

**理由**:
1. 打通现有系统，实现业务价值
2. 单元测试保障后续迭代质量

---

*建议生成完毕，供 workbuddy 参考。*